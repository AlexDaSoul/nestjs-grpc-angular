syntax = "proto3";

package api.user;

enum EUserStatus {
    USER_ACTION_UNKNOWN = 0;
    USER_ACTION_SUCCESS = 1;
    USER_ACTION_ERROR = 2;
}

message User {
    string id = 1;
    string name = 2;
    string email = 3;
    int32 role = 4;
}
